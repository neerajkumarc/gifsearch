import React from "react";
import { Form } from "react-router-dom";
import SearchBar from "../components/SearchBar";

const Home = () => {
  return (
    <main>      
      <SearchBar />
    </main>
  );
};

export default Home;
